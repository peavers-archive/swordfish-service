spring:
  application:
    name: edge-service
  cloud:
    consul:
      config:
        enabled: true
        prefix: swordfish/master/development
        defaultContext: application
        format: FILES

# Docker configuration
---
spring:
  profiles: docker
  cloud:
    consul:
      config:
        prefix: swordfish/master/production