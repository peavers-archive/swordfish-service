spring:
  application:
    name: node-service
  cloud:
    consul:
      config:
        enabled: true
        prefix: swordfish/master/
        format: FILES

# Docker configuration
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-service:8888