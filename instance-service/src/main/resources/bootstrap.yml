spring:
  application:
    name: instance-service
  cloud:
    consul:
      config:
        enabled: true
        prefix: swordfish/master/
        format: FILES

# Docker configuration
---
spring:
  profiles: docker